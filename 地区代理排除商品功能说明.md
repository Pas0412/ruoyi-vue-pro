# 地区代理排除商品功能说明

## 功能概述

地区代理排除商品功能允许管理员配置特定的商品SPU，使这些商品在订单支付后不参与地区代理佣金计算。

## 配置方式

### 1. 后台配置

在交易配置中，可以设置 `regionalAgentExcludeSpuIds` 字段，配置需要排除的商品SPU编号列表，多个编号用逗号分隔。

**示例配置：**
```
regionalAgentExcludeSpuIds: "1,2,3,10,15"
```

### 2. API接口

- **获取配置：** `GET /admin-api/trade/config/get`
- **保存配置：** `PUT /admin-api/trade/config/save`

## 订单支付后的处理流程

### 1. 正常流程（未配置排除商品）

当用户完成订单支付后，系统会：

1. **触发订单支付后处理器**
   - `TradeOrderUpdateServiceImpl.updateOrderPaid()` 方法被调用
   - 遍历所有 `TradeOrderHandler` 实现类
   - 调用每个处理器的 `afterPayOrder()` 方法

2. **地区代理佣金计算**
   - `TradeRegionalAgentOrderHandler.afterPayOrder()` 被调用
   - 检查地区代理功能是否开启
   - 获取订单中的所有商品
   - 根据收货地址计算省、市、县三级代理佣金
   - 创建佣金记录并更新代理账户

### 2. 配置排除商品后的流程

当配置了排除商品SPU后，订单支付流程会发生以下变化：

#### 步骤1：商品过滤

```java
// 过滤排除的商品
List<TradeOrderItemDO> validOrderItems = filterExcludedProducts(orderItems, tradeConfig);
if (CollUtil.isEmpty(validOrderItems)) {
    log.debug("[afterPayOrder][订单中所有商品都被排除，跳过佣金计算]");
    return;
}
```

系统会：
- 读取 `regionalAgentExcludeSpuIds` 配置
- 解析排除的商品SPU编号列表
- 过滤掉订单中属于排除列表的商品
- 只保留可参与佣金计算的商品

#### 步骤2：佣金计算

对于过滤后的商品列表：
- 如果所有商品都被排除，则跳过整个佣金计算流程
- 如果部分商品被排除，则只对剩余商品计算佣金
- 佣金计算基于商品的实际支付金额

#### 步骤3：佣金记录创建

系统会为每个有效的订单项创建佣金记录：
- 省级代理佣金记录
- 市级代理佣金记录（如果存在）
- 县级代理佣金记录（如果存在）

## 具体示例

### 示例1：部分商品被排除

**订单信息：**
- 商品A（SPU ID: 1）- 价格：100元 - **被排除**
- 商品B（SPU ID: 5）- 价格：200元 - 参与佣金
- 商品C（SPU ID: 2）- 价格：150元 - **被排除**

**配置：**
```
regionalAgentExcludeSpuIds: "1,2,3"
省级代理佣金比例: 5%
市级代理佣金比例: 3%
县级代理佣金比例: 2%
```

**处理结果：**
- 只有商品B参与佣金计算
- 省级代理获得：200 × 5% = 10元
- 市级代理获得：200 × 3% = 6元
- 县级代理获得：200 × 2% = 4元
- 商品A和商品C不产生任何佣金

### 示例2：所有商品都被排除

**订单信息：**
- 商品A（SPU ID: 1）- 价格：100元 - **被排除**
- 商品B（SPU ID: 2）- 价格：200元 - **被排除**

**配置：**
```
regionalAgentExcludeSpuIds: "1,2,3"
```

**处理结果：**
- 所有商品都被排除
- 不会产生任何地区代理佣金记录
- 系统记录日志："订单中所有商品都被排除，跳过佣金计算"

## 技术实现细节

### 1. 核心处理类

- **TradeRegionalAgentOrderHandler**：地区代理订单处理器
- **TradeConfigDO**：交易配置实体，包含排除商品配置
- **RegionalAgentRecordDO**：地区代理佣金记录实体

### 2. 关键方法

```java
// 过滤排除商品的方法
private List<TradeOrderItemDO> filterExcludedProducts(List<TradeOrderItemDO> orderItems, TradeConfigDO tradeConfig)

// 计算地区代理佣金的方法
private void calculateRegionalAgentCommission(TradeOrderDO order, List<TradeOrderItemDO> orderItems, TradeConfigDO tradeConfig)

// 创建佣金记录的方法
private void createCommissionRecords(TradeOrderDO order, List<TradeOrderItemDO> orderItems, RegionalAgentDO agent, Integer commissionPercent, Integer frozenDays, Integer agentLevel)
```

### 3. 数据库影响

- **trade_config表**：存储排除商品配置
- **regional_agent_record表**：存储佣金记录（只为非排除商品创建）
- **regional_agent表**：更新代理佣金余额（只计算非排除商品的佣金）

## 日志记录

系统会记录以下关键日志：

```
[afterPayOrder][地区代理功能未开启，跳过佣金计算]
[afterPayOrder][订单中所有商品都被排除，跳过佣金计算]
[createCommissionRecords][为代理(123)创建1级佣金记录，总金额：1000分]
```

## 注意事项

1. **配置格式**：排除商品ID必须用英文逗号分隔，不能有空格
2. **实时生效**：配置修改后立即对新订单生效
3. **历史订单**：配置修改不影响已支付订单的佣金记录
4. **佣金冻结**：排除商品不影响佣金冻结机制，非排除商品的佣金仍按配置冻结
5. **售后处理**：订单取消或售后时，只会回滚非排除商品产生的佣金记录

## 相关API文档

详细的API接口说明请参考：[地区代理功能API文档.md](./地区代理功能API文档.md)